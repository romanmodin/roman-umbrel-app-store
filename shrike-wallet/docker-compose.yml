version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: shrike-wallet_web_1
      APP_PORT: 6080

  web:
    image: shrike-umbrel
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/data:/root/.sparrow
